{
  "name": "laburen-agent-shop",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init --preview-feature",
    "seed": "node src/seed.js",
    "llm:demo": "node scripts/llm_conversation.js",
    "init:env": "powershell -ExecutionPolicy Bypass -File ./scripts/init_env.ps1"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
    "openai": "^4.4.0",
    "prisma": "^5.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
