# Ejemplo de .env para desarrollo local
# Copia este archivo a .env y completa los valores antes de ejecutar

PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/desafiolaburen
WHATSAPP_PHONE_ID=801551269711659
# Pon aquí tu token de System User (NO subir al repo). En GitHub Actions se leerá desde Secrets.
WHATSAPP_TOKEN=
WHATSAPP_VERIFY_TOKEN=mi_verify_token_local_123
LLM_API_KEY=
# PostgreSQL connection URL
DATABASE_URL=postgresql://laburen_user:MiPassSegura@localhost:5432/laburen

# Server port
PORT=3000

# WhatsApp and LLM keys (replace with your own keys in a local `.env` file)
WHATSAPP_TOKEN=your_whatsapp_token
LLM_API_KEY=your_llm_api_key
WHATSAPP_PHONE_ID=your_whatsapp_phone_id
WHATSAPP_VERIFY_TOKEN=your_webhook_verify_token
